
- defaults:
    link:
      relink: true

- clean: ['~']

- link:
     ~/.zshrc:
     ~/.gitconfig:
#    ~/.tmux.conf: tmux.conf
#    ~/.vim: vim
#    ~/.vimrc: vimrc

- create:
    - ~/Downloads
#    - ~/.vim/undo-history

- shell:
#  - [git submodule update --init --recursive, Installing submodules]
